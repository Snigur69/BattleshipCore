export enum UserErrors {
  NotExist = `User doesn't exist!`,
  AlreadyInLobby = 'User is already in lobby!',
}

export enum LobbyErrors {
  NotExist = `Lobby doesn't exist!`,
  LobbyIsFull = 'Lobby is full!',
}
